# Chess

> Ajedrez para jugar con tus amigos

## Requirements

* Python 3
* Pipenv `pip3 install pipenv`

## Setup

`pipenv install`

## Development

`pipenv install --dev`

### Run tests

`pipenv run test`

### Run linter

`pipenv run lint`
